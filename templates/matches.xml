<?xml version="1.0" encoding="UTF-8"?>
<WorlcupOdds version="$version" last_update="$last_update">
    #for $match in $matches
    <match href="$match.href">$match.teams</match>
    #end for
</WorlcupOdds>